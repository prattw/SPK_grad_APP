# Copy to .env and fill in real values. Do not commit .env.

# SMTP (for sending result emails)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USERNAME=your-email@usace.army.mil
EMAIL_PASSWORD=your-app-password

# Optional
PORT=5001
FLASK_DEBUG=false
