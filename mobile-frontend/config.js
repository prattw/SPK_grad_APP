// Backend API base URL.
// - Leave '' for same-origin (e.g. one server serving app + API) or local dev.
// - For production (GitHub Pages + Render): set to your Render backend URL after deploy, e.g.:
//   window.BACKEND_URL = 'https://spkgrad-backend.onrender.com';
window.BACKEND_URL = '';
